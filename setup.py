from setuptools import setup

setup(name='surrogate',
      version='0.1',
      description='A Python micro-lib to create stubs for non-existing modules.',
      url='http://github.com/JASchilz/surrogate',
      author='Kostia Balitsky',
      packages=['surrogate'],
      zip_safe=False)
